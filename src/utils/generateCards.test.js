import generateCards from './generateCards';

describe('generateCards', () => {
    it('returns array of double the cards of given number', () => {
        // expect(generateCards(5)).toEqual(Array(10))
    });
});